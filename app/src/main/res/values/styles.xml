<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MaterialButton.NoHighlight" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="rippleColor">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- Тема приложения -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/settings</item>
        <item name="colorControlActivated">@color/switch_thumb_active_color</item>
    </style>

    <!-- Стиль для Свитчера -->
    <style name="Color1SwitchStyle">
        <item name="colorControlActivated">@color/switch_thumb_active_color</item>
    </style>

    <!-- Стиль для Главной страницы -->
    <style name="MainButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/layout_height_0</item>
        <item name="android:layout_weight">1</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:fontFamily">@font/medium</item>
        <item name="android:gravity">center</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="cornerRadius">@dimen/padding_size</item>
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">@dimen/gap_size</item>
        <item name="iconTint">@android:color/black</item>
    </style>

    <!-- Стиль для TextView пунктов настроек -->
    <style name="SettingsItemText">
        <item name="android:layout_width">@dimen/layout_width_0</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:fontFamily">@font/regular</item>
        <item name="android:textColor">@color/letters</item>
        <item name="android:textSize">@dimen/text_size_16</item>

    </style>

    <!-- Стиль для LinearLayout пунктов -->
    <style name="SettingsItemContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingVertical">20dp</item>
        <item name="android:paddingHorizontal">16dp</item>
    </style>

    <!-- Стиль для иконок справа -->
    <style name="SettingsItemIcon">
        <item name="android:layout_width">@dimen/layout_width_24</item>
        <item name="android:layout_height">@dimen/layout_height_24</item>
    </style>



</resources>